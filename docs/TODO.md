# 待办清单（按实施计划拆解）

说明：
- 状态：`TODO` / `DOING` / `DONE` / `BLOCKED`
- 每项完成后在 `docs/DEV_LOG.md` 留存开发日志，记录变更文件与验证结果

## Phase 0：基线重构

- [x] `PH0-001` 文档化实施计划、待办清单、开发日志机制
- [x] `PH0-002` 配置模块（环境变量解析、默认值、启动配置输出）
- [x] `PH0-003` 统一错误响应（错误码、requestId、结构化错误）
- [x] `PH0-004` OpenAPI 基线文档（覆盖现有 API）
- [x] `PH0-005` 拆分 `src/server.js`：提取通用中间件与错误处理
- [x] `PH0-006` 拆分 `bookmarks/folders/plugins` 路由模块
- [x] `PH0-007` 数据层访问抽象（repository 层雏形）

## Phase 1：核心书签产品

- [x] `PH1-001` 前端状态管理抽象（当前 `public/app.js` 模块化）
- [x] `PH1-002` 书签列表分页与服务端分页 API
- [x] `PH1-003` 集合树拖拽排序与持久化
- [x] `PH1-004` 标签管理页（合并标签/重命名）
- [x] `PH1-005` 批量操作 UX 优化（确认/反馈/撤销）

## Phase 2：同步内核

- [x] `PH2-001` 同步执行日志与任务记录表（先 JSON，后 DB）
- [x] `PH2-002` 插件任务化执行器（队列前置）
- [x] `PH2-003` 同步冲突审计视图（调试信息）
- [x] `PH2-004` Chrome 扩展改为对接云端 API（替代直连 Raindrop）
- [x] `PH2-005` 同步任务重试/重放（失败任务重试、手动重放、幂等保护校验）
- [x] `PH2-006` 同步调度器（定时任务、运行窗口、并发限制、暂停/恢复）
- [x] `PH2-007` 扩展设备注册与云端配置下发（deviceId、配置拉取、状态回报）
- [x] `PH2-008` 同步健康面板（任务统计、失败趋势、最近异常摘要）

## Phase 3：采集与预览

- [x] `PH3-001` Metadata 抓取 worker（标题、描述、OG 图、favicon）
- [x] `PH3-002` 抓取任务队列与重试策略（超时、退避、失败标记）
- [x] `PH3-003` 对象存储抽象（封面图/快照/附件占位，先本地后 S3/R2）
- [x] `PH3-004` 文章提取服务（Readability 抽取正文、作者、发布时间）
- [x] `PH3-005` 预览 API（网页/PDF/图片/视频元信息与可渲染地址）
- [x] `PH3-006` 前端预览页（卡片点击预览、阅读模式入口、错误降级态）
- [x] `PH3-007` 书签卡片 metadata 展示升级（封面、摘要、类型徽标）
- [x] `PH3-008` 抓取状态可视化（抓取中/成功/失败/重试按钮）

## Phase 4：高级内容能力

- [x] `PH4-001` 高亮与注释数据模型（bookmarks/highlights/annotations）
- [x] `PH4-002` 高亮与注释 API（CRUD、定位信息、权限校验预留）
- [x] `PH4-003` 前端高亮/注释 UI（阅读模式内选区、高亮列表、编辑删除）
- [x] `PH4-004` 提醒模型与调度器（提醒时间、状态、触发记录）
- [x] `PH4-005` 提醒前端交互（设置/修改/清除提醒、状态展示）
- [x] `PH4-006` 导入器（浏览器书签 HTML）
- [x] `PH4-007` 导入器（JSON/CSV，含字段映射与冲突策略）
- [x] `PH4-008` 导出（HTML/JSON/CSV，含集合/标签/备注）
- [x] `PH4-009` 导入导出任务进度与结果报告 UI

## Phase 5：用户与协作

- [x] `PH5-001` 用户系统与认证（注册/登录/会话/token）
- [x] `PH5-002` 多租户数据隔离（userId 维度的 folders/bookmarks/plugins）
- [x] `PH5-003` 权限模型（角色、资源权限、后端鉴权中间件）
- [x] `PH5-004` 用户设置页（账号信息、token、设备会话管理）
- [x] `PH5-005` 共享集合（邀请、成员列表、角色变更）
- [x] `PH5-006` 公开页面（只读分享链接、可见性设置）
- [x] `PH5-007` 协作审计日志（关键变更记录 API + 前端查询）

## Phase 6：产品化能力

- [x] `PH6-001` 套餐能力开关（Free/Pro entitlement + 前后端 gating）
- [x] `PH6-002` 计费与订阅流程（支付集成占位、订阅状态同步）
- [x] `PH6-003` 配额统计与限制（上传/抓取/搜索等配额）
- [x] `PH6-004` 全文搜索索引（建立、更新、查询 API）
- [x] `PH6-005` 前端高级搜索 UI（筛选器、组合查询、保存查询预留）
- [x] `PH6-006` 去重检测（URL/内容指纹）
- [x] `PH6-007` 坏链检测任务（定时检测、状态回写、重试）
- [x] `PH6-008` 备份导出与恢复流程（全量快照、恢复校验）
- [x] `PH6-009` AI 整理能力（标签建议/摘要建议，先插件式接入）

## Phase 7：基础设施与发布质量（跨阶段）

- [x] `PH7-001` SQLite 数据库迁移基线（schema、迁移脚本、JSON->SQLite 数据迁移工具）
- [ ] `PH7-002` Redis/BullMQ 队列基础设施（替换进程内队列）
- [ ] `PH7-003` 对象存储接入（S3/R2 配置、上传下载签名）
- [ ] `PH7-004` 可观测性（结构化日志、错误追踪、指标埋点）
- [ ] `PH7-005` 自动化测试基线（单测/API 集成/E2E 冒烟）
- [ ] `PH7-006` 安全加固（输入校验、XSS/CSRF/速率限制、敏感信息脱敏）
- [ ] `PH7-007` CI/CD 与发布脚本（构建、测试、迁移、回滚）
- [ ] `PH7-008` 运维脚本与演练（备份恢复演练、数据修复工具）

## 当前迭代（本轮）

- [x] `ITR-001` 完成 `PH0-001` 文档落地
- [x] `ITR-002` 完成 `PH0-002` 配置模块代码
- [x] `ITR-003` 完成 `PH0-003` 统一错误响应代码
- [x] `ITR-004` 完成 `PH0-004` OpenAPI 基线文档（最小覆盖）

## Raindrop 前端对齐 TODOLIST（Web）

说明：
- 目标是“功能/交互/信息架构对标”，不直接复制品牌名、商标与素材资源
- 状态按 `DONE / DOING / TODO`
- 本组以网页端为主（`/`, `/login.html`, `/settings.html`, `/public/c/*`）

### A. 入口与导航结构

- [x] `RA-UI-001` 左上角账号入口改为侧栏用户名卡片 + 下拉菜单（设置/注销/账号）
- [x] `RA-UI-002` 未登录态直接跳转登录页（服务端 302 + 前端运行中守卫）
- [x] `RA-UI-003` 快捷入口导航图标化（图标/标题/计数结构）
- [x] `RA-UI-004` 左侧导航图标体系统一（替换临时字符图标为一致 SVG/图标字体）
- [x] `RA-UI-005` 左侧导航分组层级与间距微调（贴近 Raindrop 密度）
- [x] `RA-UI-006` 集合树节点交互优化（hover 操作、展开箭头、拖拽反馈）
- [x] `RA-UI-007` 标签区展示策略优化（更多/折叠、排序、筛选状态清晰化）

### B. 顶部工具栏与筛选

- [x] `RA-UI-101` 顶部工具栏重构（搜索、排序、批量操作、视图切换层级）
- [x] `RA-UI-102` 书签布局切换（行/卡片/标题/看板）并持久化
- [x] `RA-UI-103` 布局切换图标化与 tooltip（替换文字按钮）
- [x] `RA-UI-104` 高级筛选面板视觉重构（更紧凑、更接近 Raindrop Filter Bar）
- [x] `RA-UI-105` 已保存查询入口整合进侧栏/筛选条（而非散在按钮）
- [x] `RA-UI-106` 批量操作条状态化（仅有选中项时显示/吸顶）

### C. 书签列表/卡片/标题/看板视图

- [x] `RA-UI-201` 默认展示改为行列表（接近 Raindrop 默认）
- [x] `RA-UI-202` 行列表密度升级（主列 + 右侧信息列 + hover 操作）
- [x] `RA-UI-203` 行列表进一步列式化（标题/集合/标签/域名/时间可配置列）
- [x] `RA-UI-204` 行列表操作按钮图标化（hover 才显示文字或 tooltip）
- [x] `RA-UI-205` 卡片视图视觉收敛（边框、阴影、信息层级、封面比例）
- [x] `RA-UI-206` 标题视图极简模式优化（单行文本列表 + 快速操作）
- [x] `RA-UI-207` 看板视图（封面优先，支持无封面触发首页预览抓取）
- [x] `RA-UI-208` 看板视图瀑布流/列平衡优化（当前仍为规则网格）
- [x] `RA-UI-209` 列表项状态反馈统一（选中、hover、active、拖拽、加载中）
- [x] `RA-UI-210` 骨架屏/占位态（列表加载、封面加载、空视图）

### D. 右侧详情面板

- [x] `RA-UI-301` 详情面板分组化（基本信息/状态与提醒/抓取与阅读/高亮）
- [x] `RA-UI-302` 顶部摘要卡片（类型/标题/域名/时间/状态 chips）
- [x] `RA-UI-303` 底部操作栏 sticky 化
- [x] `RA-UI-304` 详情面板头部操作图标化（打开/预览/收藏/删除等）
- [x] `RA-UI-305` 字段编辑模式优化（查看态与编辑态分离，而非全部始终可编辑）
- [x] `RA-UI-306` 元数据/正文/抓取状态信息展示层级优化（摘要 + 展开详情）
- [x] `RA-UI-307` 高亮/注释交互细节对齐（更像注释侧栏）

### E. 预览与阅读体验

- [x] `RA-UI-401` 预览弹窗视觉重构（头部、工具栏、内容区域层级）
- [x] `RA-UI-402` 阅读模式工具条对齐（高亮、切换模式、打开原文）
- [x] `RA-UI-403` 预览加载/失败/降级态统一样式
- [x] `RA-UI-404` 预览与详情联动增强（同一条目状态同步反馈）

### F. 对话框与表单 UX

- [x] `RA-UI-501` `method="dialog"` 表单取消/关闭按钮绕过必填校验（formnovalidate）
- [x] `RA-UI-502` 新建书签/集合对话框布局与字段层级优化
- [x] `RA-UI-503` 表单校验提示统一（内联错误、字段高亮、文案风格）
- [x] `RA-UI-504` 对话框尺寸/滚动/移动端适配统一
- [x] `RA-UI-505` Toast/确认弹窗风格统一（替换原生 `confirm/prompt`，逐步收口）

### G. 登录页 / 设置页 / 公开页 对齐

- [x] `RA-UI-601` 独立登录页（非弹窗）
- [x] `RA-UI-602` 登录页视觉与信息层级进一步对齐（品牌区/表单区/说明区）
- [x] `RA-UI-603` 设置页导航与分组布局精简（更接近 Raindrop settings IA）
- [x] `RA-UI-604` 设置页面板视觉统一（卡片、分段、操作反馈）
- [x] `RA-UI-605` 公开分享页视觉重构（头部、集合信息、列表视图）

### H. 视觉系统与交互细节

- [x] `RA-UI-701` 图标系统与统一尺寸规范（SVG sprite / icon set）
- [x] `RA-UI-702` 色彩 token 微调（更接近产品质感，减少“默认蓝白”感）
- [x] `RA-UI-703` 字体与排版层级优化（标题/正文/辅助信息）
- [x] `RA-UI-704` hover/focus/active 动效统一
- [x] `RA-UI-705` 键盘快捷键与无障碍细节（列表导航、操作触发、焦点管理）
- [x] `RA-UI-706` 响应式适配细节（平板/手机布局对齐）

### I. 对齐验收与回归

- [x] `RA-UI-801` 制定页面对齐验收清单（首页/详情/预览/设置/登录/公开页）
- [x] `RA-UI-802` 截图对比流程（目标截图 vs 当前实现）
- [x] `RA-UI-803` 前端回归清单（登录、CRUD、同步、导入导出、预览）

## Raindrop 运行时 DOM 精细对齐 TODOLIST（第二轮，`/my/0`）

说明：
- 本组基于用户提供的 `app.raindrop.io/my/0` 运行时 DOM 与截图做“精细对齐”
- 范围：信息架构 / 交互形态 / 视觉层级；不直接复制品牌/商标/素材
- 状态按 `TODO / DOING / DONE`
- 与 `RA-UI-*` 的关系：`RA-UI-*` 是第一轮产品级对齐，本组是 DOM 级细化对齐（可并行维护）

### J. Split View 外壳与面板结构

- [ ] `RA-DOM-001` 左侧栏 header 结构对齐（账号按钮 + 顶部 `+` 新建入口，同一行布局）
- [ ] `RA-DOM-002` SplitView 面板边界/分隔线细化（`sidebar/main/detail` 边界、阴影、窗口 inset 头部层级）
- [ ] `RA-DOM-003` 侧栏尺寸拖拽（resize handle，宽度持久化）
- [ ] `RA-DOM-004` 详情抽屉宽度拖拽（桌面端 item panel 宽度调整）
- [ ] `RA-DOM-005` 移动端 splitview 行为对齐（侧栏/详情抽屉 overlay + backdrop + 返回逻辑）

### K. 搜索栏与顶部主工具条（Main Header）

- [x] `RA-DOM-101` 搜索框样式细化（图标、内边距、焦点态、与 Raindrop 输入高度更接近）
- [x] `RA-DOM-102` 搜索建议下拉（组合框）样式对齐：建议项 / 最近搜索 / 帮助区
- [x] `RA-DOM-103` 搜索建议键盘导航与高亮态（↑↓/Enter/Esc，ARIA combobox 行为）
- [x] `RA-DOM-104` 搜索 token 建议（标签/类型/日期/URL/备注等）与提示文案对齐
- [x] `RA-DOM-105` `询问` 按钮样式/位置对齐（AI入口，图标+文本，窄屏隐藏策略）
- [x] `RA-DOM-106` `添加` 按钮改为 split-button（主按钮 + 下拉箭头）
- [x] `RA-DOM-107` `添加` 下拉菜单（上传文件/新建书签/其他入口）样式与交互对齐

### L. 收藏集标题栏（Collection Header / Sub Header）

- [x] `RA-DOM-201` 标题栏左侧“集合图标 + 选择复选框”对齐（当前仅文字标题）
- [x] `RA-DOM-202` 标题栏 `open in browser` 微按钮对齐（图标/位置/hover）
- [x] `RA-DOM-203` 排序按钮改为 Raindrop 风格按钮（图标 + 文案 + dropdown，而非原生 `select`）
- [x] `RA-DOM-204` 视图切换入口改为菜单式按钮（“列表/卡片/标题/看板”从菜单选择）
- [x] `RA-DOM-205` 导出入口改为“更多”/菜单项形态（减少主栏按钮密度）
- [x] `RA-DOM-206` 标题栏 sticky 行为与滚动分隔阴影（接近 Raindrop 双层 header）

### M. 左侧树与过滤区（Sidebar Tree / Filters / Tags）

- [x] `RA-DOM-301` 系统集合行（所有书签/未分类/废纸篓）图标与交互细化（`more`、展开按钮占位）
- [x] `RA-DOM-302` “My Collections / 集合” section header 样式对齐（标题、更多菜单、拖拽感知）
- [x] `RA-DOM-303` 集合树行高度精确化（32px）与 icon/文字/计数/操作对齐
- [x] `RA-DOM-304` 集合树虚拟滚动（大集合数量时与 Raindrop 类似性能表现）
- [x] `RA-DOM-305` 快速过滤 section（备注/高亮/类型/无标签等）样式与菜单入口对齐
- [x] `RA-DOM-306` 标签 section header 与标签行样式对齐（micro-tag 图标、更多菜单）
- [x] `RA-DOM-307` 标签右键/更多菜单（重命名/合并/删除等）与集合菜单风格统一
- [x] `RA-DOM-308` 侧栏底部推广/状态卡区域（自定义占位，不用复制 Pro 营销文案）

### N. 列表视图（List View）精细对齐

- [x] `RA-DOM-401` 列表项结构对齐：缩略图 `56x48`、标题、副信息、动作区的精确间距/对齐
- [x] `RA-DOM-402` 列表项动作区按钮集扩展（当前 tab、新 tab、预览、Web 预览、复制、AI、收藏、标签、编辑、删除）
- [x] `RA-DOM-403` 列表项 hover / selected / active / focus 态分层与颜色微调
- [x] `RA-DOM-404` 列表项选择复选框交互与可点击区域对齐（更接近 Raindrop）
- [x] `RA-DOM-405` 列表项封面占位/加载态对齐（空图占位、比例、lazy 加载反馈）
- [x] `RA-DOM-406` 列表项域名/路径/日期信息密度和分隔符细化（中点、icon、字体权重）
- [x] `RA-DOM-407` 列表 footer “更多…” 载入模式（增量加载）与现有分页的兼容方案
- [x] `RA-DOM-408` 列表虚拟滚动（大数据量性能，对齐 Raindrop 滚动体验）

### O. Item Panel（详情抽屉）精细对齐

- [x] `RA-DOM-501` item panel 顶部工具条对齐（更多图标操作、前后项切换、关闭）
- [x] `RA-DOM-502` item panel 查看/编辑路由态对齐（`view`/`edit` 模式切换更明显）
- [x] `RA-DOM-503` item panel 与预览/阅读模式联动（从 item panel 进入 web/reader）
- [x] `RA-DOM-504` item panel 内 sections 折叠/展开交互细化（信息、标签、高亮、注释）
- [x] `RA-DOM-505` item panel 键盘导航（Esc 关闭、左右/上下切换条目）
- [x] `RA-DOM-506` item panel 打开/关闭动画与遮罩层层级细化

### P. Popover / Menu / Dialog 统一层

- [x] `RA-DOM-601` 统一 popover 容器样式（搜索建议、列表列、菜单、更多）对齐同一视觉语言
- [x] `RA-DOM-602` 菜单项高度/图标/hover/分隔线规范化（集合菜单、标签菜单、添加菜单）
- [x] `RA-DOM-603` 右键菜单定位策略优化（边界吸附、滚动容器内定位、锚点优先）
- [x] `RA-DOM-604` Dialog 与 Drawer 层级栈统一（z-index / backdrop / focus trap）

### Q. 交互与性能（可对齐体验）

- [x] `RA-DOM-701` 列表与侧栏滚动性能优化（减少全量重绘，批量 DOM 更新）
- [x] `RA-DOM-702` 书签/集合拖拽交互对齐（拖拽占位、指示线、自动展开父节点）
- [x] `RA-DOM-703` 搜索与筛选输入节流/防抖对齐（减少频繁请求与闪烁）
- [x] `RA-DOM-704` 快捷键覆盖扩展（接近 Raindrop 常用操作路径）
- [x] `RA-DOM-705` 焦点管理与无障碍细化（menu/dialog/combobox/listitem）

### R. 对齐验收（第二轮）

- [x] `RA-DOM-801` 建立 `/my/0` 页面 DOM 对照验收清单（sidebar/header/list/item panel/search suggest）
- [x] `RA-DOM-802` 截图对比（桌面宽屏/窄屏）与偏差标注流程
- [x] `RA-DOM-803` 交互回归清单（搜索、列表、详情抽屉、右键菜单、快捷键）

### S. 前端体验增强（下一步）

- [ ] `RA-FE-001` 集合树与标签区信息增强：展示收藏/未读计数、云端同步状态、折叠展开偏好与 hover 行为持久化
- [ ] `RA-FE-002` 顶部工具栏语义提示：搜索/AI 按钮显示模式徽章、批量模式吸附提示条、常驻快捷键提示
- [ ] `RA-FE-003` 辅助列表虚拟化：标签列表、快速过滤、系统集合等长列表采用虚拟滚动与懒渲染，降低大数据量卡顿
- [ ] `RA-FE-004` 主题与可访问性：新增暗色主题、字体/密度开关，并补齐 focus outline 与 ARIA 标注
- [ ] `RA-FE-005` 实时状态同步：通过 WebSocket/SSE 实时刷新同步任务、提醒、AI 结果，并配合通知中心/Toast 提示
- [ ] `RA-FE-006` AI 侧边抽屉：在主界面提供“询问 AI”抽屉，展示历史提问、标签/摘要建议及快捷操作
- [ ] `RA-FE-007` 导入导出任务面板：前端统一展示最近导入/导出任务状态、错误信息与下载入口，减少往返设置页

### T. Chrome 扩展与云服务耦合

- [ ] `CE-001` 移除 extension 对 `api.raindrop.io` 直连：后台脚本统一通过云端 `/api/plugins/raindropSync/*` 接口执行映射/同步，不再依赖 Raindrop token
- [ ] `CE-002` 优化 Options UI：用云端返回的集合/映射配置替代本地 Raindrop collection/folder 选择，隐藏 Raindrop token 输入
- [ ] `CE-003` 统一状态与设备管理：扩展侧仅读取/上报云端 device 状态，确保手动/自动同步均走服务器任务队列

## AI 能力扩展（提供商接入）

- [x] `AI-001` 后端 AI 提供商抽象（OpenAI 兼容 + Cloudflare AI）与配置存储
- [x] `AI-002` AI 自动给书签打标签（建议/写回，AI jobs 历史）
- [x] `AI-003` 设置页 AI 提供商配置（保存/刷新/测试连接/执行自动打标签）
- [x] `AI-004` 主工作台详情抽屉一键 AI 自动打标签入口（按钮 + 更多菜单）

## AI 能力扩展（产品功能赋能待办）

说明：
- 本组是在 `AI-001 ~ AI-004` 基础上继续扩展的 AI 产品能力清单
- 优先级原则：先做“提高整理效率”的功能，再做“检索/问答”，最后做“自动化/协作/评估治理”
- 状态约定：`[ ]` 未开始，`[x]` 已完成

### A. 整理与归类（高优先级，直接提升日常使用效率）

- [x] `AI-101` 批量自动打标签（选中多条书签 -> 后台任务队列 -> 分批写回）
- [x] `AI-102` 标签标准化建议（同义标签合并建议，如 `AI`/`人工智能`/`ai`）
- [x] `AI-103` 集合推荐（根据标题/域名/标签建议目标集合，支持一键移动）
- [x] `AI-104` 标题清洗与重命名建议（去站点噪声、保留关键信息）
- [x] `AI-105` 自动摘要生成（为无备注书签补摘要/备注）
- [x] `AI-106` 语言识别与标签本地化（中英文标签统一策略，避免标签碎片化）
- [x] `AI-107` 新书签保存时自动分类规则（保存后自动打标签/推荐集合，可确认后应用）

### B. 检索与发现（高优先级，提升“找回信息”的能力）

- [x] `AI-201` 自然语言搜索转高级筛选（将“找最近一周关于天气 API 的中文文章”转为查询条件）
- [x] `AI-202` 语义搜索（向量索引 + 相似内容召回，与关键词搜索并存）
- [x] `AI-203` 书签问答（RAG over bookmarks/article/highlights，回答时给出处链接）
- [x] `AI-204` 结果重排序（关键词命中结果再用 AI 重排，提升前几条相关性）
- [x] `AI-205` 语义去重聚类（近重复页面/同主题多版本页面聚类）
- [x] `AI-206` “相关书签推荐”（基于当前书签展示相关条目）

### C. 阅读与知识沉淀（中高优先级，放大已实现的阅读/高亮能力）

- [x] `AI-301` 阅读模式摘要（短摘要 / 关键要点 / 适合收藏理由）
- [x] `AI-302` 高亮候选生成（从正文自动推荐可高亮片段）
- [x] `AI-303` 高亮聚合总结（按书签汇总高亮与注释，生成知识卡片）
- [x] `AI-304` 集合知识摘要（对一个集合生成主题总结、常见标签、代表来源）
- [x] `AI-305` 周报/日报 Digest（按时间窗口汇总新增收藏 + AI 摘要）
- [x] `AI-306` 阅读优先级建议（根据主题、时效性、长度、历史行为给排序）

### D. 自动化与工作流（中优先级，结合现有任务/插件体系）

- [x] `AI-401` AI 规则动作引擎（触发器：新增书签/抓取完成 -> 动作：打标签/移动/摘要）
- [x] `AI-402` 历史库 AI 回填任务（为旧书签分批补标签/摘要，支持暂停/恢复）
- [ ] `AI-403` Provider 路由与降级策略（主模型失败时切备选模型；Cloudflare 优先免费模型）
- [ ] `AI-404` 成本/配额护栏（按用户、按任务、按日调用限额；大任务限速）
- [ ] `AI-405` Prompt 模板管理（标签、摘要、问答、分类等模板可配置/版本化）
- [ ] `AI-406` AI 任务审计面板（任务状态、耗时、成功率、失败原因、模型分布）

### E. 协作与公开分享（中优先级，增强团队与公开页价值）

- [ ] `AI-501` 公开集合 AI 导览页（集合简介、主题标签、FAQ、推荐入口）
- [ ] `AI-502` 协作集合变更摘要（每天/每周总结新收藏与重点内容）
- [ ] `AI-503` 团队标签规范建议（团队共享集合内标签命名建议与冲突提示）
- [ ] `AI-504` 评论/注释辅助（为共享场景生成讨论提示或摘要）

### F. 质量、评估与治理（高优先级但可并行基础设施化）

- [ ] `AI-601` Prompt/Eval 基线（建立标签/摘要评估样本集与回归评分）
- [ ] `AI-602` AI 输出质量反馈机制（用户接受/拒绝/修改建议，形成反馈数据）
- [ ] `AI-603` 安全与隐私策略（发送给模型前的字段裁剪、可选匿名化/脱敏）
- [ ] `AI-604` Provider 健康检查（测试连接、延迟、错误率、可用性面板）
- [ ] `AI-605` AI 功能开关与权限（按用户/角色/套餐启用不同 AI 能力）

### G. 建议实施顺序（给后续开发排期）

- [x] `AI-901` Phase 1：`AI-101` 批量自动打标签 + `AI-104` 标题清洗 + `AI-105` 自动摘要
- [x] `AI-902` Phase 2：`AI-201` 自然语言搜索转筛选 + `AI-203` 书签问答（带出处）
- [x] `AI-903` Phase 3：`AI-401` AI 规则动作引擎 + `AI-402` 历史库回填任务
- [ ] `AI-904` Phase 4：`AI-601~AI-605` 评估治理与成本/健康体系
