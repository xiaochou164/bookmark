# 待办清单（按实施计划拆解）

说明：
- 状态：`TODO` / `DOING` / `DONE` / `BLOCKED`
- 每项完成后在 `docs/DEV_LOG.md` 留存开发日志，记录变更文件与验证结果

## Phase 0：基线重构

- [x] `PH0-001` 文档化实施计划、待办清单、开发日志机制
- [x] `PH0-002` 配置模块（环境变量解析、默认值、启动配置输出）
- [x] `PH0-003` 统一错误响应（错误码、requestId、结构化错误）
- [x] `PH0-004` OpenAPI 基线文档（覆盖现有 API）
- [x] `PH0-005` 拆分 `src/server.js`：提取通用中间件与错误处理
- [x] `PH0-006` 拆分 `bookmarks/folders/plugins` 路由模块
- [x] `PH0-007` 数据层访问抽象（repository 层雏形）

## Phase 1：核心书签产品

- [x] `PH1-001` 前端状态管理抽象（当前 `public/app.js` 模块化）
- [x] `PH1-002` 书签列表分页与服务端分页 API
- [x] `PH1-003` 集合树拖拽排序与持久化
- [x] `PH1-004` 标签管理页（合并标签/重命名）
- [x] `PH1-005` 批量操作 UX 优化（确认/反馈/撤销）

## Phase 2：同步内核

- [x] `PH2-001` 同步执行日志与任务记录表（先 JSON，后 DB）
- [x] `PH2-002` 插件任务化执行器（队列前置）
- [x] `PH2-003` 同步冲突审计视图（调试信息）
- [x] `PH2-004` Chrome 扩展改为对接云端 API（替代直连 Raindrop）
- [x] `PH2-005` 同步任务重试/重放（失败任务重试、手动重放、幂等保护校验）
- [x] `PH2-006` 同步调度器（定时任务、运行窗口、并发限制、暂停/恢复）
- [x] `PH2-007` 扩展设备注册与云端配置下发（deviceId、配置拉取、状态回报）
- [x] `PH2-008` 同步健康面板（任务统计、失败趋势、最近异常摘要）

## Phase 3：采集与预览

- [x] `PH3-001` Metadata 抓取 worker（标题、描述、OG 图、favicon）
- [x] `PH3-002` 抓取任务队列与重试策略（超时、退避、失败标记）
- [x] `PH3-003` 对象存储抽象（封面图/快照/附件占位，先本地后 S3/R2）
- [x] `PH3-004` 文章提取服务（Readability 抽取正文、作者、发布时间）
- [x] `PH3-005` 预览 API（网页/PDF/图片/视频元信息与可渲染地址）
- [x] `PH3-006` 前端预览页（卡片点击预览、阅读模式入口、错误降级态）
- [x] `PH3-007` 书签卡片 metadata 展示升级（封面、摘要、类型徽标）
- [x] `PH3-008` 抓取状态可视化（抓取中/成功/失败/重试按钮）

## Phase 4：高级内容能力

- [x] `PH4-001` 高亮与注释数据模型（bookmarks/highlights/annotations）
- [x] `PH4-002` 高亮与注释 API（CRUD、定位信息、权限校验预留）
- [x] `PH4-003` 前端高亮/注释 UI（阅读模式内选区、高亮列表、编辑删除）
- [x] `PH4-004` 提醒模型与调度器（提醒时间、状态、触发记录）
- [x] `PH4-005` 提醒前端交互（设置/修改/清除提醒、状态展示）
- [x] `PH4-006` 导入器（浏览器书签 HTML）
- [x] `PH4-007` 导入器（JSON/CSV，含字段映射与冲突策略）
- [x] `PH4-008` 导出（HTML/JSON/CSV，含集合/标签/备注）
- [x] `PH4-009` 导入导出任务进度与结果报告 UI

## Phase 5：用户与协作

- [x] `PH5-001` 用户系统与认证（注册/登录/会话/token）
- [ ] `PH5-002` 多租户数据隔离（userId 维度的 folders/bookmarks/plugins）
- [ ] `PH5-003` 权限模型（角色、资源权限、后端鉴权中间件）
- [ ] `PH5-004` 用户设置页（账号信息、token、设备会话管理）
- [ ] `PH5-005` 共享集合（邀请、成员列表、角色变更）
- [ ] `PH5-006` 公开页面（只读分享链接、可见性设置）
- [ ] `PH5-007` 协作审计日志（关键变更记录 API + 前端查询）

## Phase 6：产品化能力

- [ ] `PH6-001` 套餐能力开关（Free/Pro entitlement + 前后端 gating）
- [ ] `PH6-002` 计费与订阅流程（支付集成占位、订阅状态同步）
- [ ] `PH6-003` 配额统计与限制（上传/抓取/搜索等配额）
- [ ] `PH6-004` 全文搜索索引（建立、更新、查询 API）
- [ ] `PH6-005` 前端高级搜索 UI（筛选器、组合查询、保存查询预留）
- [ ] `PH6-006` 去重检测（URL/内容指纹）
- [ ] `PH6-007` 坏链检测任务（定时检测、状态回写、重试）
- [ ] `PH6-008` 备份导出与恢复流程（全量快照、恢复校验）
- [ ] `PH6-009` AI 整理能力（标签建议/摘要建议，先插件式接入）

## Phase 7：基础设施与发布质量（跨阶段）

- [ ] `PH7-001` PostgreSQL 数据库迁移（schema、迁移脚本、数据迁移工具）
- [ ] `PH7-002` Redis/BullMQ 队列基础设施（替换进程内队列）
- [ ] `PH7-003` 对象存储接入（S3/R2 配置、上传下载签名）
- [ ] `PH7-004` 可观测性（结构化日志、错误追踪、指标埋点）
- [ ] `PH7-005` 自动化测试基线（单测/API 集成/E2E 冒烟）
- [ ] `PH7-006` 安全加固（输入校验、XSS/CSRF/速率限制、敏感信息脱敏）
- [ ] `PH7-007` CI/CD 与发布脚本（构建、测试、迁移、回滚）
- [ ] `PH7-008` 运维脚本与演练（备份恢复演练、数据修复工具）

## 当前迭代（本轮）

- [x] `ITR-001` 完成 `PH0-001` 文档落地
- [x] `ITR-002` 完成 `PH0-002` 配置模块代码
- [x] `ITR-003` 完成 `PH0-003` 统一错误响应代码
- [x] `ITR-004` 完成 `PH0-004` OpenAPI 基线文档（最小覆盖）
